<resources>
    <style name="Theme.NoaTamagotchi" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Si quieres StatusBar transparente, etc.: -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <!-- Si no usas action bar (Compose), esto ya la quita -->
    </style>
</resources>
